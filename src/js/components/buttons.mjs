/**
 * @module components/buttons
 * @type {HTMLElement}
 * Represents the sign-in button element.
 * signInButton = document.getElementById('sign_in-button');
 * Represents the sign-up button element.
 * signUpButton = document.getElementById('sign_up-button');
 */

const signInButton = document.getElementById('sign_in-button');

const signUpButton = document.getElementById('sign_up-button');

export { signInButton, signUpButton };